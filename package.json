{
  "name": "be11-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Premium Cricket Ground Booking Platform - Monorepo",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=@be11/shared && npm run build --workspace=be11-backend && npm run build --workspace=be11-frontend",
    "vercel-build": "npm run build --workspace=@be11/shared && npm run build --workspace=be11-backend && npm run build --workspace=be11-frontend",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,css,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,css,md}\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.19.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
